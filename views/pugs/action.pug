extends ../layouts/main.pug 

include ../mixins/action

block content
    
    table.table.table-dark
        thead.thead-dark
            tr
                th(scope="col").h5.font-weight-bold # Username
                th(scope="col").h5.font-weight-bold # Operation
                th(scope="col").h5.font-weight-bold # Date
        tbody.tbody-dark
            if objects !== null
                each object in objects
                    +createList(object)
            else
                 